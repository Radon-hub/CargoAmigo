name: Continuous Integration

on:
  push:
    branches:
      - develop
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Start
        run: echo "Start building project!"

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show files
        run: ls -la